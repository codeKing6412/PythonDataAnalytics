# Carseats 多元线性回归分析

这次使用 `Carseats` 数据，响应变量为 `Sales`，选取：

- `Price`
- `Income`
- `Advertising`
- `ShelveLoc`

作为解释变量。

其中 `ShelveLoc` 是定性变量，分为 `Bad`、`Good`、`Medium`。

## 1. 加载数据

我这里使用 ISLP 包里面自带的 Carseats 数据。

如果没有安装可以先：

```bash
pip install ISLP statsmodels
```

Python 代码：

```python
import pandas as pd
import statsmodels.formula.api as smf

from ISLP import load_data

Carseats = load_data("Carseats")

print(Carseats.head())
print(Carseats.shape)
print(Carseats["ShelveLoc"].value_counts())
```

数据一共有 400 条记录。

为了防止分类变量的基准组不明确，我把 `Bad` 放在第一个：

```python
Carseats["ShelveLoc"] = pd.Categorical(
    Carseats["ShelveLoc"],
    categories=["Bad", "Good", "Medium"]
)
```

---

## 2. 建立多元线性回归模型

模型为：

\[
Sales=
\beta_0+
\beta_1Price+
\beta_2Income+
\beta_3Advertising+
\beta_4ShelveLoc_{Good}+
\beta_5ShelveLoc_{Medium}+
\varepsilon
\]

Python：

```python
model = smf.ols(
    "Sales ~ Price + Income + Advertising + ShelveLoc",
    data=Carseats
).fit()

print(model.summary())
```

我得到的主要拟合结果如下：

| 变量 | coef | std err | t |
|---|---:|---:|---:|
| Intercept | 10.3916 | 0.5213 | 19.9325 |
| Price | -0.0570 | 0.0037 | -15.4800 |
| Income | 0.0137 | 0.0031 | 4.3993 |
| Advertising | 0.1056 | 0.0131 | 8.0438 |
| ShelveLoc[T.Good] | 4.8359 | 0.2597 | 18.6243 |
| ShelveLoc[T.Medium] | 1.8959 | 0.2132 | 8.8945 |

模型的：

```text
R-squared          ≈ 0.6268
Adjusted R-squared ≈ 0.6221
F-statistic        ≈ 132.36
Residual df        = 394
```

所以最后大概可以写成回归方程：

\[
\hat{Sales}
=
10.3916
-0.0570Price
+0.0137Income
+0.1056Advertising
+4.8359I(Good)
+1.8959I(Medium)
\]

这里 \(I(Good)\) 和 \(I(Medium)\) 是虚拟变量。

---

## 3. ShelveLoc 的基准组

从模型输出可以看到：

```text
ShelveLoc[T.Good]
ShelveLoc[T.Medium]
```

但是没有：

```text
ShelveLoc[T.Bad]
```

所以可以判断：

\[
\boxed{ShelveLoc\text{ 的基准组是 Bad}}
\]

原因是一个有三种类别的定性变量，在模型包含截距项的情况下，只需要设置两个虚拟变量。

这里实际上是：

| ShelveLoc | Good dummy | Medium dummy |
|---|---:|---:|
| Bad | 0 | 0 |
| Good | 1 | 0 |
| Medium | 0 | 1 |

因此所有关于 `Good` 和 `Medium` 的系数，都是相对于 `Bad` 来解释。

---

## 4. ShelveLoc[Good] 系数的商业含义

模型中：

\[
\hat\beta_{Good}=4.8359
\]

`Sales` 的单位是千件，因此这个系数的含义是：

> 在 Price、Income 和 Advertising 保持相同的情况下，如果一家商店的货架位置从 Bad 变为 Good，那么模型预测的儿童座椅销量平均增加约 4.836 千件。

也就是大约：

\[
4.8359\times1000\approx4836
\]

件。

所以从这个模型看，货架摆放位置和销售额之间存在很明显的关系。

至少在控制了价格、地区收入和广告投入以后，`Good` 的货架位置对应的预测销量仍然明显高于 `Bad`。

这里需要注意，我觉得不能直接说：

> “把货架改成 Good 一定导致销量增加 4836 件。”

因为线性回归这里体现的是统计上的关联关系，不一定能直接证明因果关系。

---

## 5. 计算 VIF

接下来计算各个变量的 VIF。

先把 `ShelveLoc` 转换成虚拟变量：

```python
from statsmodels.stats.outliers_influence import variance_inflation_factor
import statsmodels.api as sm

X = Carseats[
    ["Price", "Income", "Advertising", "ShelveLoc"]
].copy()

X = pd.get_dummies(
    X,
    columns=["ShelveLoc"],
    drop_first=True,
    dtype=float
)

print(X.head())
```

由于前面规定了 `Bad` 为第一个类别，所以 `drop_first=True` 会把 `Bad` 删除，最后留下：

```text
Price
Income
Advertising
ShelveLoc_Good
ShelveLoc_Medium
```

加入常数项：

```python
X_vif = sm.add_constant(X)

vif_result = pd.DataFrame()

vif_result["Variable"] = X_vif.columns

vif_result["VIF"] = [
    variance_inflation_factor(X_vif.values, i)
    for i in range(X_vif.shape[1])
]

print(vif_result)
```

在判断解释变量共线性的时候，不考虑常数项 `const`。

计算结果大约为：

| 变量 | VIF |
|---|---:|
| Price | 1.008 |
| Income | 1.012 |
| Advertising | 1.009 |
| ShelveLoc_Good | 1.497 |
| ShelveLoc_Medium | 1.494 |

可以看到所有解释变量的 VIF 都比较低。

一般情况下：

```text
VIF 接近 1    共线性很弱
VIF > 5       需要开始注意
VIF > 10      通常认为存在比较严重的多重共线性
```

这里最大的 VIF 也只有大约：

\[
1.50
\]

所以我认为：

\[
\boxed{\text{这个模型没有明显的多重共线性问题}}
\]

`ShelveLoc_Good` 和 `ShelveLoc_Medium` 的 VIF 比三个连续变量略高一点，这主要是因为它们本身都是由同一个三分类变量 `ShelveLoc` 转换出来的，但目前这个数值还是很低的。

---

## 6. 最后的简单总结

这次模型的 \(R^2\) 大约是：

\[
R^2=0.627
\]

说明 Price、Income、Advertising 和 ShelveLoc 这几个变量合起来，大约能够解释 Sales 样本变异的 62.7%。

几个比较明显的结果：

1. `Price` 系数为负，价格越高，预测销量越低；
2. `Income` 系数为正；
3. `Advertising` 系数为正；
4. 相比 Bad，Good 和 Medium 的货架位置对应销量都更高；
5. 其中 Good 相比 Bad 的差距大约是 4.836 千件；
6. 所有解释变量 VIF 都小于 2，因此没有发现明显多重共线性。

完整代码合起来如下：

```python
import pandas as pd
import statsmodels.api as sm
import statsmodels.formula.api as smf

from ISLP import load_data
from statsmodels.stats.outliers_influence import variance_inflation_factor


# 1. 读取数据
Carseats = load_data("Carseats")

print(Carseats.head())
print(Carseats.shape)


# 2. 设置分类变量顺序
Carseats["ShelveLoc"] = pd.Categorical(
    Carseats["ShelveLoc"],
    categories=["Bad", "Good", "Medium"]
)


# 3. 建立回归模型
model = smf.ols(
    "Sales ~ Price + Income + Advertising + ShelveLoc",
    data=Carseats
).fit()

print(model.summary())


# 4. VIF
X = Carseats[
    ["Price", "Income", "Advertising", "ShelveLoc"]
].copy()

X = pd.get_dummies(
    X,
    columns=["ShelveLoc"],
    drop_first=True,
    dtype=float
)

X_vif = sm.add_constant(X)

vif_result = pd.DataFrame({
    "Variable": X_vif.columns,
    "VIF": [
        variance_inflation_factor(X_vif.values, i)
        for i in range(X_vif.shape[1])
    ]
})

print(vif_result)
```