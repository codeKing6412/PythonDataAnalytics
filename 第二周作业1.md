# 线性回归作业 1、2

## 1. 一元线性回归残差的两个性质

一元线性回归模型写成：

\[
y_i=\beta_0+\beta_1x_i+\varepsilon_i
\]

拟合之后：

\[
\hat y_i=\hat\beta_0+\hat\beta_1x_i
\]

残差为：

\[
e_i=y_i-\hat y_i
=y_i-\hat\beta_0-\hat\beta_1x_i
\]

OLS 的目标是使残差平方和最小：

\[
RSS=\sum_{i=1}^{n}(y_i-\beta_0-\beta_1x_i)^2
\]

下面分别对 \(\beta_0\) 和 \(\beta_1\) 求偏导。

### （1）证明残差之和为 0

对 \(\beta_0\) 求偏导：

\[
\frac{\partial RSS}{\partial \beta_0}
=
-2\sum_{i=1}^{n}(y_i-\beta_0-\beta_1x_i)
\]

最小二乘估计量处偏导应该为 0，所以：

\[
-2\sum_{i=1}^{n}
(y_i-\hat\beta_0-\hat\beta_1x_i)=0
\]

括号中的部分实际上就是残差 \(e_i\)，因此：

\[
\sum_{i=1}^{n}e_i=0
\]

所以证明得到：

\[
\boxed{\sum_{i=1}^{n}e_i=0}
\]

也就是说，在含有截距项的一元线性回归中，正残差和负残差最后会相互抵消。

另外也可以理解为：

\[
\bar e=0
\]

即样本残差的平均数为 0。

> 注意：这里有一个前提，就是回归模型中包含截距项。如果强制模型不设置截距，那么这个性质不一定成立。

---

### （2）证明残差与自变量乘积之和为 0

同样，对 RSS 中的 \(\beta_1\) 求偏导：

\[
\frac{\partial RSS}{\partial \beta_1}
=
-2\sum_{i=1}^{n}
x_i(y_i-\beta_0-\beta_1x_i)
\]

令偏导为 0：

\[
-2\sum_{i=1}^{n}
x_i(y_i-\hat\beta_0-\hat\beta_1x_i)=0
\]

因为：

\[
e_i=y_i-\hat\beta_0-\hat\beta_1x_i
\]

所以：

\[
\sum_{i=1}^{n}x_ie_i=0
\]

最终得到：

\[
\boxed{\sum_{i=1}^{n}x_ie_i=0}
\]

我的理解是，OLS 在寻找最佳拟合直线的时候，不仅使残差总体平均为 0，同时也让残差和解释变量 \(x\) 正交。

从矩阵形式看其实就是：

\[
X^Te=0
\]

一元线性回归只是这个性质的特殊情况。

---

## 2. 为什么增加随机无关变量后 \(R^2\) 不下降，而调整 \(R^2\) 可能下降？

首先普通的 \(R^2\) 定义为：

\[
R^2=1-\frac{RSS}{TSS}
\]

其中：

- RSS 是残差平方和；
- TSS 是总离差平方和。

假设原来的模型中有 \(k\) 个解释变量，现在加入一个随机生成、实际上和 \(y\) 没有什么关系的新变量 \(z\)。

### 普通 \(R^2\) 为什么不会下降？

加入变量之后，新模型实际上包含了旧模型。

因为新模型可以写成：

\[
y=\beta_0+\beta_1x_1+\cdots+\beta_kx_k+\beta_{k+1}z+\varepsilon
\]

如果新变量 \(z\) 完全没有作用，那么至少可以令：

\[
\beta_{k+1}=0
\]

这时候就直接退化成原模型。

但是 OLS 会重新寻找使 RSS 最小的一组系数，所以新模型的 RSS 最坏也只能和旧模型相同：

\[
RSS_{new}\le RSS_{old}
\]

由于 TSS 并没有变化：

\[
R^2_{new}
=
1-\frac{RSS_{new}}{TSS}
\]

因此：

\[
\boxed{R^2_{new}\ge R^2_{old}}
\]

所以普通 \(R^2\) 有一个问题：只要不断往模型里面添加变量，它几乎永远不会变差，即使这个变量实际上没什么用。

---

## 调整 \(R^2\) 为什么会下降？

调整 \(R^2\) 会把模型使用掉的自由度考虑进去。

如果模型中有 \(k\) 个解释变量：

\[
R_{adj}^2
=
1-
\frac{RSS/(n-k-1)}
{TSS/(n-1)}
\]

这里：

\[
n-k-1
\]

就是残差自由度。

假设增加一个新变量，则残差自由度变成：

\[
n-k-2
\]

虽然 RSS 可能稍微下降，但是我们同时损失了一个自由度。

原模型使用：

\[
\frac{RSS_{old}}{n-k-1}
\]

来估计残差方差。

加入一个变量之后变成：

\[
\frac{RSS_{new}}{n-k-2}
\]

因为分母也减少了 1，如果新变量只能让 RSS 降低一点点，那么这种 RSS 的改善不足以补偿失去的自由度。

于是可能出现：

\[
\frac{RSS_{new}}{n-k-2}
>
\frac{RSS_{old}}{n-k-1}
\]

从而：

\[
R_{adj,new}^2<R_{adj,old}^2
\]

这也是调整 \(R^2\) 相比普通 \(R^2\) 更适合比较解释变量数量不同模型的原因。

---

### 用 RSS 下降量再理解一下

假设原来的残差自由度为：

\[
d=n-k-1
\]

增加一个变量后变为 \(d-1\)。

如果想让调整 \(R^2\) 上升，需要：

\[
\frac{RSS_{new}}{d-1}
<
\frac{RSS_{old}}{d}
\]

整理之后，相当于要求：

\[
RSS_{old}-RSS_{new}
>
\frac{RSS_{old}}{d}
\]

右边其实就是旧模型的残差均方 MSE。

也就是说：

> 新变量带来的 RSS 降低必须“大到足以补偿一个自由度的损失”。

纯随机变量和响应变量本来没有实际关系，它一般只能因为样本中的偶然相关性吸收一点残差，因此 RSS 虽然下降，但是下降得往往不够多，所以调整 \(R^2\) 通常会下降。

### 一个小的补充

课堂上一般会说：

> 加入无关变量，\(R^2\) 上升，而调整 \(R^2\) 下降。

不过严格来说，后半句话不是百分之百必然。

因为随机变量有时候也可能恰好和当前残差产生比较大的偶然相关，这时 RSS 下降得比较多，调整 \(R^2\) 也有可能上升。

因此更准确的表述应该是：

- \(R^2\)：一定上升或保持不变；
- 调整 \(R^2\)：如果变量带来的 RSS 改善不足以抵消自由度损失，就会下降，对于纯随机无关变量通常会出现这种情况。

这也是调整 \(R^2\) 中“惩罚无意义复杂度”的体现。